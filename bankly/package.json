{
  "name": "bankly",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "seed": "dropdb -f --if-exists bankly && createdb bankly && psql bankly < data.sql && dropdb -f --if-exists bankly_test && createdb bankly_test && psql bankly_test < data.sql",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.16.4",
    "jsonwebtoken": "^8.3.0",
    "pg": "^8.11.3",
    "supertest": "^3.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
